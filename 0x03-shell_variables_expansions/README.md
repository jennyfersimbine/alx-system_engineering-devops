read file containig scripts
ls - rm * is to delete all in current dir
echo "hello $user" - prints hello for current user
export PATH = "$PATH:/action" - creates dir in path and sets to the end
find $PATH ...
printenv
set - print local variables
